<!DOCTYPE html>
<html lang="it" class="h-100">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profilo - SkillBridge</title>
    <%- include('partials/head') %>
    <script src="/js/navbar.js" defer></script>
</head>

<body class="d-flex flex-column h-100 has-navbar-padding">

    <%- include('partials/navbar') %>
    <%- include('partials/modal') %>

    <!-- Main content -->
    <main class="flex-shrink-0">
        <div class="container py-5">
            <!-- Header del profilo -->
            <div class="p-4 card shadow-sm mb-4">
                <div class="row align-items-center">                    <div class="col-md-4 text-center text-md-start mb-3 mb-md-0">
                        <img src="/img/profile.png" alt="Foto Profilo" class="rounded-circle profile-image mb-3">                        <div>
                            <% if (user.type === 'freelancer') { %>
                                <span class="badge bg-primary mb-2">Freelancer</span>
                                <h1 class="h3 mb-2 fw-bold"><%= user.firstName %> <%= user.lastName %></h1>
                                <p class="text-muted mb-2">@<%= user.username %></p>
                                <% if (user.location) { %>
                                <div class="mt-2">
                                    <i class="bi bi-geo-alt text-secondary me-1"></i>
                                    <span class="text-secondary"><%= user.location %></span>
                                </div>
                                <% } %>
                            <% } else if (user.type === 'business') { %>
                                <h1 class="h3 mb-2 fw-bold"><%= user.businessName %></h1>
                                <p class="text-muted mb-2">@<%= user.username %></p>
                                <span class="badge bg-secondary">Azienda</span>
                                <% if (user.location) { %>
                                <div class="mt-2">
                                    <i class="bi bi-geo-alt text-secondary me-1"></i>
                                    <span class="text-secondary"><%= user.location %></span>
                                </div>
                                <% } %>
                                <% if (user.industry) { %>
                                <div class="mt-1">
                                    <i class="bi bi-briefcase text-secondary me-1"></i>
                                    <span class="text-secondary"><%= user.industry %></span>
                                </div>
                                <% } %>
                            <% } %>
                        </div>
                    </div>
                    <div class="col-md-8">
                        <div class="d-flex justify-content-between align-items-start mb-3">
                            <h4 class="fw-bold mb-0">Informazioni</h4>
                            <% if (user.type === 'freelancer') { %>
                                <% if (user.hourlyRate) { %>
                                <div class="text-end">
                                    <span class="badge bg-success p-2 fs-6">€<%= user.hourlyRate %>/ora</span>
                                </div>
                                <% } %>
                            <% } %>
                        </div>
                        
                        <p><%= user.bio || 'Sono uno sviluppatore web full-stack con oltre 5 anni di esperienza nella creazione di soluzioni web moderne e responsive. La mia passione è costruire interfacce utente intuitive e accessibili che offrano un\'ottima esperienza utente.' %></p>
                        
                        <% if (user.type === 'freelancer' && user.experience) { %>
                        <div class="mt-3">
                            <h6 class="fw-bold"><i class="bi bi-award me-2"></i>Esperienza</h6>
                            <p class="mb-0"><%= user.experience %> anni di esperienza</p>
                        </div>
                        <% } %>
                        
                        <% if (user.type === 'business' && user.foundedYear) { %>
                        <div class="mt-3">
                            <h6 class="fw-bold"><i class="bi bi-calendar-check me-2"></i>Anno di fondazione</h6>
                            <p class="mb-0"><%= user.foundedYear %></p>
                        </div>
                        <% } %>
                    </div>
                </div>
            </div>
            
            <div class="row">
                <div class="col-lg-8">
                    <!-- Competenze Card -->
                    <div class="card shadow-sm mb-4">
                        <div class="card-body p-4">
                            <h4 class="mb-3 fw-bold">Competenze</h4>
                            <div class="d-flex flex-wrap gap-2">
                                <span class="badge rounded-pill bg-primary">HTML5</span>
                                <span class="badge rounded-pill bg-primary">CSS3</span>
                                <span class="badge rounded-pill bg-primary">JavaScript</span>
                                <span class="badge rounded-pill bg-primary">TypeScript</span>
                                <span class="badge rounded-pill bg-primary">React</span>
                                <span class="badge rounded-pill bg-primary">Node.js</span>
                                <span class="badge rounded-pill bg-primary">Express.js</span>
                                <span class="badge rounded-pill bg-primary">Git</span>
                                <span class="badge rounded-pill bg-primary">Bootstrap</span>
                                <span class="badge rounded-pill bg-primary">Responsive Design</span>
                            </div>
                        </div>
                    </div>
                      <!-- Annunci Pubblicati Card -->
                    <div class="card shadow-sm">
                        <div class="card-body p-4">
                            <h4 class="mb-3 fw-bold">Annunci Pubblicati</h4>
                            <% if (userPosts && userPosts.length > 0) { %>
                                <% userPosts.forEach(post => { %>
                                    <!-- Annuncio -->
                                    <div class="card mb-3">
                                        <div class="card-body">
                                            <h5 class="card-title mb-2 fw-bold"><%= post.title %></h5>
                                            <p class="card-text mb-1">
                                                <small class="text-muted">
                                                    Pubblicato il: <%= new Date(post.createdAt).toLocaleDateString('it-IT') %>
                                                </small>
                                            </p>
                                            <p class="card-text"><%= post.content %></p>
                                        </div>
                                    </div>
                                <% }); %>                            <% } else { %>
                                <div class="alert alert-secondary">
                                    Nessun annuncio pubblicato.
                                </div>
                            <% } %>
                        </div>
                    </div>
                </div>
                
                <div class="col-lg-4">
                    <!-- Card Contatti -->
                    <div class="card shadow-sm mb-4">
                        <div class="card-body p-4">
                            <h4 class="mb-3 fw-bold">Contatti</h4>                            <ul class="list-group list-group-flush">                                <li class="list-group-item px-0 border-0">
                                    <div class="d-flex align-items-center">
                                        <i class="bi bi-envelope text-primary me-2"></i>
                                        <div>
                                            <p class="mb-0"><%= user.email %></p>
                                        </div>
                                    </div>
                                </li>
                                <% if (user.website) { %>
                                <li class="list-group-item px-0 border-0">
                                    <div class="d-flex align-items-center">
                                        <i class="bi bi-globe text-primary me-2"></i>
                                        <div>
                                            <a href="<%= user.website %>" class="text-primary" target="_blank"><%= user.website %></a>
                                        </div>
                                    </div>
                                </li>
                                <% } %>
                                <li class="list-group-item px-0 border-0">
                                    <div class="d-flex align-items-center">
                                        <i class="bi bi-telephone text-primary me-2"></i>
                                        <div>
                                            <p class="mb-0">+39 123 456 7890</p>
                                        </div>
                                    </div>
                                </li>
                            </ul>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <%- include('partials/footer') %>

</body>
</html>