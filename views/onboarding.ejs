<!DOCTYPE html>
<html lang="it" class="h-100">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Onboarding - SkillBridge</title>
    <%- include('partials/head') %>
</head>

<body class="gradient-bg">

    <!-- Main content -->
    <div class="container min-vh-100 d-flex align-items-center justify-content-center py-5">
        <div class="card shadow-sm form-container">
            <div class="card-body p-4">
                <div class="d-flex justify-content-between align-items-center mb-2">
                    <h1 class="card-title fw-bold">Benvenuto!</h1>
                    <form action="/onboarding/skip" method="POST">
                        <button type="submit" class="btn btn-link text-muted d-flex align-items-center skip-link">
                            <span>Salta</span>
                            <i class='bi bi-arrow-right'></i>
                        </button>
                    </form>
                </div>
                <p class="text-muted text-start mb-4">Ancora pochi passi per completare il tuo profilo.</p>
                <% if (error_msg && error_msg.length > 0) { %>
                    <div class="alert alert-danger">
                        <%= error_msg %>
                    </div>
                <% } %>
                <% if (info_msg && info_msg.length > 0) { %>
                    <div class="alert alert-info">
                        <%= info_msg %>
                    </div>
                <% } %>
                <% if (success_msg && success_msg.length > 0) { %>
                    <div class="alert alert-success">
                        <%= success_msg %>
                    </div>
                <% } %>
                
                <form action="/onboarding" method="POST">
                    <!-- Selezione tipo account -->
                    <div class="mb-4">
                        <p class="fw-bold mb-3">Seleziona il tipo di account:</p>
                        <div class="row g-3">
                            <div class="col-6">
                                <label class="option card h-100 cursor-pointer" for="type-freelancer">
                                    <input type="radio" name="type" value="freelancer" id="type-freelancer" class="form-check-input position-absolute top-0 end-0 m-2" checked>
                                    <div class="card-body text-center py-4">
                                        <i class='bi bi-person-fill fs-1 mb-2'></i>
                                        <div class="fw-bold">Freelancer</div>
                                    </div>
                                </label>
                            </div>
                            <div class="col-6">
                                <label class="option card h-100 cursor-pointer" for="type-business">
                                    <input type="radio" name="type" value="business" id="type-business" class="form-check-input position-absolute top-0 end-0 m-2">
                                    <div class="card-body text-center py-4">
                                        <i class='bi bi-buildings-fill fs-1 mb-2'></i>
                                        <div class="fw-bold">Azienda</div>
                                    </div>
                                </label>
                            </div>
                        </div>
                    </div>
                    
                    <!-- Campi comuni -->
                    <div class="input-group mb-3">
                        <span class="input-group-text"><i class="bi bi-person-circle"></i></span>
                        <input type="text" class="form-control" name="name" placeholder="Nome Completo / Nome Azienda" autocomplete="name">
                    </div>
                    <div class="input-group mb-3">
                        <span class="input-group-text"><i class="bi bi-globe"></i></span>
                        <input type="url" class="form-control" name="website" placeholder="Sito Web (opzionale)" autocomplete="url">
                    </div>
                    <div class="input-group mb-3">
                        <span class="input-group-text"><i class="bi bi-telephone-fill"></i></span>
                        <input type="tel" class="form-control" name="phone" placeholder="Telefono (opzionale)" autocomplete="tel">
                    </div>
                    <div class="input-group mb-3">
                        <span class="input-group-text"><i class="bi bi-geo-alt-fill"></i></span>
                        <input type="text" class="form-control" name="location" placeholder="Località (opzionale)" autocomplete="address-level2">
                    </div>
                    <div class="text-center">
                        <button type="submit" class="btn btn-primary mt-2">Completa registrazione</button>
                    </div>
                </form>
            </div>
        </div>
    </div>

</body>
</html>