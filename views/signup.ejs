<!DOCTYPE html>
<html lang="it">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Registrati - SkillBridge</title>
    <%- include('partials/head') %>
    <script src="/js/scripts.js" defer></script>
</head>

<body class="auth-page">

    <!-- Main content -->
    <div class="container-fluid vh-100">
        <div class="row h-100">
            <!-- Sezione Sinistra -->
            <div class="col-md-6 p-4 auth-container">
                <div class="mx-auto form-container">
                    <div class="mb-4">
                        <a href="/" class="text-decoration-none d-flex align-items-center">
                            <img src="/favicon.ico" alt="Logo" class="me-2 logo-small" />
                            <span class="fs-5 text-dark fw-bold auth-form-logo">SkillBridge</span>
                        </a>
                    </div>
                    <h1 class="mb-2 fw-bold">Crea un account</h1>
                    <p class="text-muted mb-4">Registrati e inizia subito.</p>
                    <form action="#" method="POST">
                        <div class="input-group mb-3">
                            <span class="input-group-text"><i class="bi bi-envelope-fill"></i></span>
                            <input type="email" class="form-control" id="email" name="email" placeholder="Email" required autocomplete="email">
                        </div>
                        <div class="input-group mb-3">
                            <span class="input-group-text"><i class="bi bi-tag-fill"></i></span>
                            <input type="text" class="form-control" id="username" name="username" placeholder="Username" required autocomplete="username" pattern="[a-z0-9-]+" minlength="3" maxlength="30" title="Solo lettere minuscole, numeri e trattini.">
                            <button class="btn btn-outline-secondary" type="button" data-bs-toggle="tooltip" data-bs-placement="top" data-bs-trigger="hover focus" tabindex="-1" title="L'username può contenere solo lettere, numeri e trattini.">
                                <i class="bi bi-question-circle-fill"></i>
                            </button>
                        </div>
                        <div class="input-group mb-3">
                            <span class="input-group-text"><i class="bi bi-lock-fill"></i></span>
                            <input type="password" class="form-control" id="password" name="password" placeholder="Password" required>
                            <button type="button" class="btn btn-outline-secondary toggle-password" tabindex="-1">
                                <i class="bi bi-eye-fill"></i>
                            </button>
                        </div>
                        <div class="input-group mb-3">
                            <span class="input-group-text"><i class="bi bi-lock-fill"></i></span>
                            <input type="password" class="form-control" id="confirm-password" name="confirm-password" placeholder="Conferma Password" required>
                            <button type="button" class="btn btn-outline-secondary toggle-password" tabindex="-1">
                                <i class="bi bi-eye-fill"></i>
                            </button>
                        </div>
                        <div class="form-check mb-3">
                            <input type="checkbox" class="form-check-input" id="agree-terms" name="agree-terms" required>
                            <label class="form-check-label" for="agree-terms">
                                Accetto i <a href="#" class="auth-link">termini di servizio</a>
                            </label>
                        </div>
                        <% if (error_msg && error_msg.length > 0) { %>
                            <div class="alert alert-danger">
                                <%= error_msg %>
                            </div>
                        <% } %>
                        <% if (success_msg && success_msg.length > 0) { %>
                            <div class="alert alert-success">
                                <%= success_msg %>
                            </div>
                        <% } %>
                        <button type="submit" class="btn btn-primary w-100 mb-3">Registrati</button>
                        <p class="text-center">Hai già un account? <a href="/login" class="auth-link">Effettua il login</a></p>
                        <div class="text-center mt-3">
                            <a href="/" class="auth-link d-inline-flex align-items-center">
                                Continua come ospite<i class="bi bi-arrow-right ms-1"></i> 
                            </a>
                        </div>
                    </form>
                </div>
            </div>
            <!-- Sezione Destra -->
            <div class="col-md-6 gradient-bg p-4 d-flex flex-column justify-content-center">
                <div class="text-center mb-4">
                    <img src="/img/handshake.png" alt="Handshake" class="img-fluid mb-4 auth-form-image" />
                </div>
                <div class="text-center px-4">
                    <h2 class="mb-3 fw-bold">Il tuo ponte verso la crescita professionale</h2>
                    <p>SkillBridge offre un ambiente dinamico dove freelance e aziende collaborano per innovare e crescere.</p>
                </div>
            </div>
        </div>
    </div>
    
</body>
</html>