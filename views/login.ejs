<!DOCTYPE html>
<html lang="it">
    
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Accedi - SkillBridge</title>
    <%- include('partials/head') %>
    <script src="/js/login-signup.js" defer></script>
</head>

<body class="auth-page">

    <!-- Main content -->
    <div class="container-fluid vh-100">
        <div class="row h-100">
            <!-- Left Section -->
            <div class="col-md-6 p-4 auth-container">
                <div class="mx-auto form-container">
                    <div class="mb-4">
                        <a href="/" class="text-decoration-none d-flex align-items-center">
                            <img src="/favicon.ico" alt="Logo" class="me-2 logo-small" />
                            <span class="fs-5 text-dark fw-bold login-logo-text">SkillBridge</span>
                        </a>
                    </div>
                    <h1 class="mb-2 fw-bold">Accedi</h1>
                    <p class="text-muted mb-4">Riprendi da dove hai lasciato.</p>
                    <form action="#" method="POST">
                        <div class="input-group mb-3">
                            <span class="input-group-text"><i class="bi bi-at"></i></span>
                            <input type="text" class="form-control" name="emailOrUsername" placeholder="Email o Username" required autocomplete="email">
                        </div>
                        <div class="input-group mb-3">
                            <span class="input-group-text"><i class="bi bi-lock"></i></span>
                            <input type="password" class="form-control" id="password" name="password" placeholder="Password" required>
                            <button type="button" class="btn btn-outline-secondary toggle-password" tabindex="-1">
                                <i class="bi bi-eye"></i>
                            </button>
                        </div>
                        <% if (error_msg && error_msg.length > 0) { %>
                            <div class="alert alert-danger">
                                <%= error_msg %>
                            </div>
                        <% } %>
                        <% if (error && error.length > 0) { %>
                            <div class="alert alert-danger">
                                <%= error %>
                            </div>
                        <% } %>
                        <% if (success_msg && success_msg.length > 0) { %>
                            <div class="alert alert-success">
                                <%= success_msg %>
                            </div>
                        <% } %>
                        <button type="submit" class="btn btn-primary w-100 mb-3">Accedi</button>
                        <p class="text-center">Non hai un account? <a href="/signup" class="auth-link">Registrati</a></p>
                    </form>
                </div>
            </div>
            <!-- Right Section -->
            <div class="col-md-6 gradient-bg p-4 d-flex flex-column justify-content-center">
                <div class="text-center mb-4">
                    <img src="/img/handshake.png" alt="Handshake" class="img-fluid mb-4 banner-image" />
                </div>
                <div class="text-center px-4">
                    <h2 class="mb-3 fw-bold">Il tuo ponte verso la crescita professionale</h2>
                    <p>SkillBridge offre un ambiente dinamico dove freelance e aziende collaborano per innovare e crescere.</p>
                </div>
            </div>
        </div>
    </div>

</body>
</html>